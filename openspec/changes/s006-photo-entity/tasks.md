## 1. PhotoEntity immutable class defined（PhotoEntity 不可變類別設計）

- [x] 1.1 在 `lib/data/models/photo_entity.dart` 中實作 PhotoEntity immutable class defined：定義 `PhotoEntity` 類別，提供 `const` 建構子
- [x] 1.2 宣告必填 `final` 欄位：`id`（String）、`url`（String）、`filename`（String）、`blogTitle`（String），使用 `required` named parameters
- [x] 1.3 宣告選填 `final` 欄位：`width`（int?）、`height`（int?）
