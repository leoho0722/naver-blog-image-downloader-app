## 1. AppConfig class defined（AppConfig 靜態配置類別）

- [x] 1.1 在 `lib/config/app_config.dart` 中實作 AppConfig class defined：定義 `AppConfig` 為 `abstract final class`
- [x] 1.2 宣告 `static const String baseUrl`，設定 Lambda API base URL

## 2. Constants class defined（Constants 常數類別）

- [x] 2.1 在 `lib/utils/constants.dart` 中實作 Constants class defined：定義 `Constants` 為 `abstract final class`
- [x] 2.2 宣告 `static const String appName`，設定應用程式名稱
